# 文森 AI 管理系统 V4.2.2 基线

V4.2.2 是基于 V4.2 的统一布局优化版本，不改变项目、工时和审批业务逻辑。

## 本次调整

- 商机管理：搜索、阶段、负责人三个控件等宽排列。
- 合同管理：搜索、状态两个控件等宽排列。
- 项目管理：搜索、状态、负责人三个控件等宽排列。
- 流程审批：搜索、类型、状态三个控件等宽排列。
- 记录工作：修复弹窗横向溢出、关闭按钮错位和底部操作被截断。
- 创建流程：修复弹窗横向溢出；窄屏下流程类型使用 2×2 排列，表单切换为单列。

## 响应式规则

- 常规桌面宽度：筛选控件均分整行。
- 窄屏宽度：筛选控件依次纵向排列。
- 弹窗宽度始终限制在当前可视区域内，内容区可独立纵向滚动。

## 继承范围

- 完整继承 V4.2 的个人中心、业务、执行和审批功能。
- 财务管理和数据中心仍沿用原有版本，未新增业务范围。
