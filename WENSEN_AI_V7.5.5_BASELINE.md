# 文森 AI 管理系统 V7.5.5 正式基线

## 基础版本

- 基础版本：V7.5.2。
- 本地开发文件：`prototype-v7.5.5.html`。
- V7.5.2 文件保持不变，用于回退。

## 发布状态

- 当前状态：已通过 GitHub App/API 写入远程 `main`，并由 GitHub Pages 自动发布。
- Pages 地址：`https://racheleleanorzhang-q.github.io/AILaw2026/prototype-v7.5.5.html`。

## 菜单结构

- 沿用 V7.5.2 的菜单与业务管理 Tab：业务概览、潜在业务、项目台账、合同台账。
- 本次不调整一级菜单及项目详情 Tab 顺序。

## 新增与调整功能

1. 项目详情“财务台账 → 应收计划”只显示“添加应收计划”。
2. 项目详情“财务台账 → 开票记录”显示“发起开票申请”和“登记发票”。
3. 项目详情“财务台账 → 回款记录”只显示“回款登记”。
4. 项目详情“财务台账 → 核销明细”不显示新增或申请按钮。
5. 统一压缩“发起申请”弹窗的标题栏、正文、字段、分类按钮和底部操作区间距。
6. 统一发起流程删除用户可见的“申请标题”和“期望完成日期”。
7. 申请标题改由系统结合关联对象和审批事项自动生成。
8. 审批管理、工作台及项目财务台账等调用统一发起流程组件的入口同步生效。
9. 开票申请的“项目、合同、分摊至应收期次”调整为同一行三列展示，并保持三级联动。
10. 项目详情“合同”Tab 取消合同详情中间页；点击合同名称直接打开当前有效的最新合同文件预览，双签版优先。

## 核心数据链路

- 应收计划 → 发起开票申请 → 审批通过 → 登记发票 → 回款登记 → 核销明细。
- 项目财务台账各子页面按钮直接进入对应业务操作，不再使用含义宽泛的统一“发起申请”按钮。
- 所有审批入口继续写入同一审批数据，并沿用现有审批状态、审批链路和详情回写逻辑。

## 已确认业务原则

- 应收计划只负责新增应收计划。
- 开票记录同时提供开票申请和实际发票登记入口。
- 回款记录只负责登记实际到账回款。
- 核销明细只用于查看，不提供新增按钮。
- 发起人不再手工填写申请标题和期望完成日期。
- 项目合同列表以合同文件为直接查看对象，不再从列表进入原合同详情页。

## 与 V7.5.2 的差异

- 按财务业务动作重新分配项目财务台账各子页按钮。
- 统一精简并压缩所有入口调用的发起申请表单。
- 收拢开票申请业务关联字段，并简化项目合同文件的查看路径。
